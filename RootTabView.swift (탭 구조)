import SwiftUI   // SwiftUI 프레임워크 불러오기 (iOS 앱의 UI를 선언적으로 구성하기 위해 사용)

// 앱의 하단 탭(Tab) 구조를 담당하는 뷰
// '오늘', '기록', '설정' 세 개의 화면을 탭으로 전환할 수 있다.
struct RootTabView: View {
    // SwiftUI에서 화면의 시각적 구조를 정의하는 필수 프로퍼티
    var body: some View {
        // TabView: 여러 개의 화면(View)을 탭 형태로 전환할 수 있게 하는 컨테이너
        TabView {
            // 첫 번째 탭 - "오늘" 화면
            DashboardView()   // DashboardView: 오늘의 물 섭취 진행률과 버튼이 있는 메인 화면
                // tabItem: 탭에 표시될 아이콘과 텍스트를 정의
                .tabItem {
                    Label("오늘", systemImage: "drop.fill")   // Label: 텍스트("오늘") + 시스템 이미지(물방울 아이콘)
                }

            // 두 번째 탭 - "기록" 화면
            HistoryView()     // HistoryView: 시간별 섭취 내역을 리스트 형태로 보여주는 화면
                .tabItem {
                    Label("기록", systemImage: "clock.fill")  // 시계 아이콘과 "기록" 텍스트로 표시
                }

            // 세 번째 탭 - "설정" 화면
            SettingsView()    // SettingsView: 하루 목표 섭취량 등 사용자 설정을 관리하는 화면
                .tabItem {
                    Label("설정", systemImage: "gearshape.fill") // 톱니바퀴 아이콘 + "설정" 텍스트
                }
        }
        // TabView는 내부의 각 View를 자동으로 탭으로 인식하고
        // 사용자가 화면 하단의 탭 바를 통해 화면을 전환할 수 있게 한다.
    }
}
